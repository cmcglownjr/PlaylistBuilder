<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:PlaylistBuilder.GUI.ViewModels"
             xmlns:models="clr-namespace:PlaylistBuilder.GUI.Models"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="PlaylistBuilder.GUI.Views.PlaylistView">
    <Design.DataContext>
        <vm:PlaylistViewModel/>
    </Design.DataContext>
    <StackPanel>
        <DataGrid AutoGenerateColumns="True" Items="{Binding PlaylistTracks}"/>
        <!-- <DataGrid Name="PlaylistDataGrid" Items="{Binding PlaylistTracks}" AutoGenerateColumns="False"> -->
        <!--     <DataGrid.Columns> -->
        <!--         <DataGridTextColumn Header="Track #" -->
        <!--                             Binding="{Binding Track}" -->
        <!--                             Width="2*"/> -->
        <!--         <DataGridTextColumn Header="Title" -->
        <!--                             Binding="{Binding Title}" -->
        <!--                             Width="2*"/> -->
        <!--         <DataGridTextColumn Header="Artist" -->
        <!--                             Binding="{Binding Artist}" -->
        <!--                             Width="3*"/> -->
        <!--         <DataGridTextColumn Header="Album" -->
        <!--                             Binding="{Binding Album}" -->
        <!--                             Width="3*"/> -->
        <!--         <DataGridTextColumn Header="Duration" -->
        <!--                             Binding="{Binding Duration}" -->
        <!--                             Width="1*"/> -->
        <!--         <DataGridTextColumn Header="File Name" -->
        <!--                             Binding="{Binding FileName}" -->
        <!--                             Width="3*"/> -->
        <!--     </DataGrid.Columns> -->
        <!-- -->
        <!-- </DataGrid> -->
    </StackPanel>
</UserControl>
